name: Staging Auto-Update (Velopack/Sparkle)

# Triggered on every push to `staging`. Builds the rolling-release
# artefacts that Velopack (Win) + Sparkle (Mac) auto-update consume
# from the `staging-latest` GitHub release. Counterpart to
# `staging-tester.yml` (which is tag-driven and produces a SIGNED
# tester installer side-by-side with prod). The two used to share
# the display name "Staging Release" — renamed 2026-05-14 to remove
# the ambiguity in the Actions UI.
on:
  push:
    branches: [staging]
    # Doc-only / mockup / markdown commits don't need a 30-min
    # installer build. The actual sources we ship live under
    # core/, platforms/, scripts/, .github/. Anything else can be
    # committed without burning CI runners.
    paths-ignore:
      - 'docs/**'
      - 'mockup/**'
      - '**/*.md'
      - 'CLAUDE.md'
  workflow_dispatch:

permissions:
  contents: write

# Concurrency — re-introduced 2026-05-17 (cancel-in-progress=true).
# The original 0bd2209 revert was driven by doc-tweak commits killing
# long-running builds; that's now solved by the paths-ignore filter
# above. The actual incident TODAY: two back-to-back CODE pushes
# (4bec281 warnings, ff4a46c changelog) each spawned a run; both
# called `gh release delete staging-latest` at the start, then
# `Create GitHub Release` (draft), built artefacts, and tried to
# `Publish release` (PATCH draft→public) at the end. The later
# delete nuked the earlier draft, so the earlier run's PATCH 404'd.
# Without serial/cancel semantics any rapid code push reproduces.
# cancel-in-progress is the right pick here: a newer commit
# supersedes the older one's artefacts anyway (same staging-latest
# tag), so finishing the older build wastes runner minutes.
concurrency:
  group: staging-auto-update
  cancel-in-progress: true

jobs:
  # ── Rust core lint + test (runs in parallel with builds) ────────────
  rust-check:
    name: Rust Lint & Test
    runs-on: ubuntu-22.04
    steps:
      - uses: actions/checkout@v5

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          components: rustfmt, clippy

      - name: Rust cache
        uses: Swatinem/rust-cache@v2
        with:
          workspaces: core

      - name: Install Linux dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y libasound2-dev libxdo-dev libdbus-1-dev libssl-dev cmake

      - name: Format
        working-directory: core
        run: cargo fmt --check

      - name: Clippy
        working-directory: core
        run: cargo clippy --features local-stt -- -D warnings

      - name: Test
        working-directory: core
        run: cargo test --lib --features local-stt

  # ── Create release placeholder (no dependency on rust-check) ────────
  create-release:
    name: Create Staging Release
    runs-on: ubuntu-latest
    outputs:
      release_id: ${{ steps.create.outputs.id }}
      version: ${{ steps.version.outputs.version }}
    steps:
      - uses: actions/checkout@v5
        with:
          fetch-depth: 0

      - name: Get version
        id: version
        run: |
          VER=$(grep '^version' core/Cargo.toml | head -1 | sed 's/.*"\(.*\)"/\1/')
          SHA=$(git rev-parse --short HEAD)
          echo "version=${VER}-native.${SHA}" >> "$GITHUB_OUTPUT"

      - name: Generate changelog
        run: |
          # Previously used `git merge-base HEAD origin/main` but
          # `origin/main` in this repo is the long-stale historical
          # branch (the PR base is `feat/native-ui`, see CLAUDE.md).
          # That produced a 22 k-char release body listing 50+ commits
          # going back weeks — every staging-native publish reposted
          # the same wall of text. Fix: mirror release.yml's logic
          # and use the latest published version tag as the changelog
          # base. Falls back to the last 20 commits if no tag exists
          # (first-ever build / fresh fork).
          PREV_TAG=$(git tag --sort=-creatordate --list 'v*' | grep -vE -- '-staging\.' | head -n 1)
          if [ -n "$PREV_TAG" ]; then
            ALL=$(git log --pretty=format:"- %s" --no-merges "${PREV_TAG}..HEAD" 2>/dev/null || echo "- (no commits since ${PREV_TAG})")
            HEADER="### Changes since \`${PREV_TAG}\`"
          else
            ALL=$(git log --pretty=format:"- %s" --no-merges -20 HEAD)
            HEADER="### Recent changes"
          fi
          TOTAL=$(printf '%s\n' "$ALL" | grep -c . || true)
          FIRST=$(printf '%s\n' "$ALL" | head -n 10)
          REST=$(printf '%s\n' "$ALL" | tail -n +11)
          {
            echo "## Staging Build (rolling)"
            echo ""
            echo "**Rolling pre-release. Updated on every push to \`staging\`. Not for production.**"
            echo ""
            echo "Same code as the next tagged \`v*-rcN\` release will ship — only useful for"
            echo "manual smoke-test by the team. Velopack auto-update does NOT pick this up"
            echo "(rolling tag isn't semver). For the staging-tester install with side-by-side"
            echo "Dimmy-Staging + Stripe Test, see the \`v*-staging.N\` releases instead."
            echo ""
            echo "$HEADER"
            echo ""
            echo "$FIRST"
            if [ -n "$REST" ]; then
              MORE=$((TOTAL - 10))
              echo ""
              echo "<details><summary><b>$MORE more commit(s)…</b></summary>"
              echo ""
              echo "$REST"
              echo ""
              echo "</details>"
            fi
            echo ""
            echo "## Downloads"
            echo ""
            echo "| Platform | File |"
            echo "|----------|------|"
            echo "| macOS (Apple Silicon) | \`Dimmy-native-macos-arm64.dmg\` |"
            echo "| Windows (installer) | \`Dimmy-win-Setup.exe\` |"
            echo "| Windows (portable) | \`Dimmy-win-Portable.zip\` |"
            echo "| Linux (GTK4) | \`Dimmy-native-linux-x86_64.AppImage\` |"
            echo ""
            echo "## Windows"
            echo ""
            echo "Self-contained — no .NET runtime or VC++ redist install needed."
            echo "- **Setup.exe**: Installer (recommended) — installs VC++ redist automatically"
            echo "- **Portable.zip**: Extract and run anywhere"
            echo ""
            echo "## macOS Installation"
            echo ""
            echo "This build is not notarized by Apple. After copying Dimmy.app to Applications:"
            echo ""
            echo "\`\`\`bash"
            echo "xattr -d com.apple.quarantine /Applications/Dimmy.app"
            echo "\`\`\`"
            echo ""
            echo "Or: right-click the app → Open → Open in the dialog."
          } > changelog.md

      - name: Delete previous native staging release
        continue-on-error: true
        run: gh release delete staging-latest --yes --cleanup-tag
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Create GitHub Release
        id: create
        uses: softprops/action-gh-release@v3
        with:
          tag_name: staging-latest
          name: "Dimmy ${{ steps.version.outputs.version }} (Native Staging)"
          body_path: changelog.md
          draft: true
          prerelease: true

  # ── macOS Native Build (parallel with Windows + rust-check) ─────────
  build-macos:
    name: Build macOS Native
    needs: create-release
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v5

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: aarch64-apple-darwin

      - name: Install CMake
        run: |
          brew list cmake >/dev/null 2>&1 || brew install cmake
          cmake --version

      - name: Verify Swift toolchain (need 5.10+ for FluidAudio)
        run: |
          # FluidAudio's Package.swift declares swift-tools-version:5.10.
          # macos-latest runner ships Xcode 15.3+ → Swift 5.10+. Print
          # the version explicitly so a future runner downgrade fails
          # this step instead of mid-build.
          swift --version
          xcodebuild -version | head -2

      - name: Fetch onnxruntime arm64 dylib (Parakeet ort path)
        # The Xcode build phase needs the dylib in core/target/onnxruntime-osx-arm64-1.22.0/.
        # Pre-fetch here so the Xcode step is offline-quick.
        run: scripts/download-onnxruntime.sh

      - name: Rust cache
        uses: Swatinem/rust-cache@v2
        with:
          workspaces: core
          shared-key: macos-staging

      - name: Cache FluidAudio Swift build
        uses: actions/cache@v5
        with:
          # `swift build` runs as part of fluidaudio-rs's build.rs and
          # rebuilds 500+ Swift files (~5 min cold) for FluidAudio +
          # NIO + Collections + Argument Parser. Cache the OUT_DIR
          # subdir + SwiftPM repo cache so warm CI runs are <30s.
          path: |
            ~/Library/Developer/Xcode/DerivedData/FluidAudio*
            ~/Library/Caches/org.swift.swiftpm
            core/target/aarch64-apple-darwin/release/build/fluidaudio-rs-*
          key: fluidaudio-${{ runner.os }}-${{ hashFiles('core/Cargo.lock') }}
          restore-keys: |
            fluidaudio-${{ runner.os }}-

      - name: Build Rust static library
        working-directory: core
        env:
          # Telemetry secrets — embedded into the binary at build time via
          # core/build.rs. PostHog analytics + Sentry crash reporting +
          # logs both active. Sentry 0.47 with default-features = false
          # + explicit native-tls (schannel on Windows) avoids the
          # rustls/CryptoProvider static-init panic that __fastfailed
          # the cdylib under WindowsAppSDK + Velopack (V3 / V8).
          POSTHOG_API_KEY: ${{ secrets.POSTHOG_API_KEY }}
          SENTRY_DSN: ${{ secrets.SENTRY_DSN }}
          # staging credentials — pubkey + URL are public by design (see
          # .env.staging committed to the repo). Hardcoding inline avoids
          # depending on a secrets.DIMMY_LICENSE_PUBKEY_STAGING entry
          # that may not be set on this repo's GitHub Secrets — the
          # private signing key lives only in Cloudflare's staging Worker
          # secret store, never in the public binary's verify pubkey.
          # build.rs invariant (commit 8a71f30) requires pubkey + URL
          # to move together; flavor=staging triggers ~/.config/dimmy-
          # staging isolation + UI watermark.
          DIMMY_LICENSE_PUBKEY: avlM65dRGDHomOzqGICiuIXf6_dY21w653AOnza2fzU
          DIMMY_LICENSE_SERVER_URL: https://license-staging.dimmy.app
          DIMMY_BUILD_FLAVOR: staging
        run: |
          echo "CMake: $(cmake --version | head -1)"
          echo "Xcode CLT: $(xcode-select -p)"
          cargo clean --release 2>/dev/null
          # local-stt-parakeet-fluid: Apple Neural Engine path via
          # FluidInference's CoreML bundle. Target-scoped to macOS
          # arm64 so the feature has no effect on Win/Linux jobs.
          cargo build --lib --release --target aarch64-apple-darwin \
            --features local-stt-metal,local-llm-metal,local-stt-parakeet-coreml,local-stt-parakeet-fluid,local-dfn,license-client

      - name: Verify static library
        run: |
          ls -lh core/target/aarch64-apple-darwin/release/libdimmy_lib.a
          file core/target/aarch64-apple-darwin/release/libdimmy_lib.a
          rm -f core/target/aarch64-apple-darwin/release/libdimmy_lib.dylib
          rm -f core/target/aarch64-apple-darwin/release/deps/libdimmy_lib.dylib
          echo "=== llama/ggml dylibs in release dir ==="
          ls -la core/target/aarch64-apple-darwin/release/libllama* core/target/aarch64-apple-darwin/release/libggml* 2>/dev/null || echo "No dylibs found"

      - name: Build Xcode project
        working-directory: platforms/macos
        run: |
          # Derive the version from core/Cargo.toml so CFBundleShortVersionString
          # matches the staging tag — without this override xcodebuild falls back
          # to MARKETING_VERSION in the .pbxproj (0.1.0), which is what users saw
          # in the sidebar footer of every staging DMG. release.yml already does
          # this; staging-auto-update.yml didn't, so the two pipelines silently shipped
          # different version strings.
          VERSION=$(grep -m1 '^version = ' ../../core/Cargo.toml | sed -E 's/.*"(.*)".*/\1/')
          if [ -z "$VERSION" ]; then echo "::error::could not parse version from core/Cargo.toml"; exit 1; fi
          echo "Building Dimmy.app with MARKETING_VERSION=$VERSION"
          xcodebuild -project Dimmy.xcodeproj \
            -scheme Dimmy \
            -configuration Release \
            -derivedDataPath build/derived \
            MARKETING_VERSION="$VERSION" \
            CURRENT_PROJECT_VERSION="$VERSION" \
            build \
            CODE_SIGN_IDENTITY="-"

      - name: Bundle llama.cpp dylibs
        run: |
          APP_PATH="platforms/macos/build/derived/Build/Products/Release/Dimmy.app"
          FRAMEWORKS_DIR="$APP_PATH/Contents/Frameworks"
          mkdir -p "$FRAMEWORKS_DIR"
          RELEASE_DIR="core/target/aarch64-apple-darwin/release"

          # Copy dylibs dereferencing symlinks so every file is a real binary
          for name in libllama libggml libggml-base libggml-cpu libggml-metal; do
            for dylib in "$RELEASE_DIR"/${name}*.dylib; do
              [ -e "$dylib" ] || continue
              DEST="$FRAMEWORKS_DIR/$(basename "$dylib")"
              cp -RL "$dylib" "$DEST"
              install_name_tool -id "@rpath/$(basename "$dylib")" "$DEST" 2>/dev/null || true
            done
          done

          # Re-sign all dylibs
          codesign --force --sign - "$FRAMEWORKS_DIR"/*.dylib

          echo "=== Frameworks contents ==="
          ls -la "$FRAMEWORKS_DIR/"
          echo "=== Verifying no broken symlinks ==="
          find "$FRAMEWORKS_DIR" -type l ! -exec test -e {} \; -print | grep . && echo "BROKEN SYMLINKS FOUND" && exit 1 || echo "All OK"

      # See release.yml for context.
      - name: Build dimmy-mcp for aarch64-apple-darwin
        working-directory: mcp-server
        run: cargo build --release --target aarch64-apple-darwin

      - name: Copy dimmy-mcp into .app Resources
        run: |
          APP_PATH="platforms/macos/build/derived/Build/Products/Release/Dimmy.app"
          RESOURCES_DIR="$APP_PATH/Contents/Resources"
          cp "mcp-server/target/aarch64-apple-darwin/release/dimmy-mcp" "$RESOURCES_DIR/dimmy-mcp"
          chmod +x "$RESOURCES_DIR/dimmy-mcp"
          codesign --force --sign - "$RESOURCES_DIR/dimmy-mcp"

      - name: Generate DMG background (SVG → PNG)
        run: |
          brew install librsvg
          rsvg-convert -w 660 -h 400 platforms/macos/dmg-assets/dmg-background.svg -o platforms/macos/dmg-assets/dmg-background.png

      - name: Create DMG
        working-directory: platforms/macos
        run: |
          # create-dmg's `--app-drop-link` only positions a visual
          # icon — it does NOT create the underlying `Applications`
          # symlink when invoked with a single .app as source. We
          # stage the .app + an explicit `ln -s /Applications` into
          # `build/dmg-contents/`, then point create-dmg at the
          # directory. Regression introduced 2026-04-16 (commit
          # c75b5b5) when the build migrated from `hdiutil + ln -s`
          # to create-dmg without preserving the symlink step;
          # confirmed missing in v*-staging.5.
          brew install create-dmg
          APP_PATH="build/derived/Build/Products/Release/Dimmy.app"
          mkdir -p build/dmg-contents
          rm -rf build/dmg-contents/Dimmy.app
          cp -R "$APP_PATH" build/dmg-contents/
          ln -sf /Applications build/dmg-contents/Applications
          create-dmg \
            --volname "Dimmy" \
            --background "dmg-assets/dmg-background.png" \
            --window-pos 200 120 \
            --window-size 660 400 \
            --icon-size 128 \
            --icon "Dimmy.app" 165 200 \
            --app-drop-link 495 200 \
            --no-internet-enable \
            "build/Dimmy-native-macos-arm64.dmg" \
            "build/dmg-contents"

      - name: Upload DMG to release
        uses: softprops/action-gh-release@v3
        with:
          tag_name: staging-latest
          files: platforms/macos/build/Dimmy-native-macos-arm64.dmg

  # ── Windows Native Build (parallel with macOS + rust-check) ─────────
  build-windows:
    name: Build Windows Native
    # Run in parallel with Mac + Linux. Earlier we gated on Mac to
    # save the 30-40 min Win runner budget when Mac fails early —
    # but the failure modes are disjoint (Win has its own toolchain
    # invariants, Mac has its own Xcode/Swift quirks), so a Mac
    # failure doesn't actually predict a Win failure. Parallelism
    # cuts ~15 min off the staging wall-clock when everything is
    # green; the cost when Mac fails is ~30 min of wasted Win
    # runner — acceptable.
    needs: [create-release]
    runs-on: windows-2025-vs2026
    steps:
      - uses: actions/checkout@v5

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: x86_64-pc-windows-msvc

      - name: Install CMake & Ninja
        uses: lukka/get-cmake@latest

      - name: Install VS 2026 BuildTools side-by-side (MSVC 14.50+)
        shell: pwsh
        run: |
          # windows-2025 ships VS 2022 Enterprise only (MSVC 14.44, which
          # miscompiles whisper.cpp's ggml-vulkan state-init path). MSVC
          # 14.50+ exists only in VS 2026 (VS 18). Install path: Microsoft
          # has not yet registered aka.ms/vs/18/* short-urls (they all
          # redirect to Bing), so the official bootstrapper has no public
          # direct URL. Chocolatey hosts the preview package that internally
          # fetches the signed installer from Microsoft's CDN. Install it
          # side-by-side with VS 2022 — VS 2022 stays authoritative for the
          # .NET publish + UWP AppxPackage PRI generation steps because the
          # BuildTools SKU lacks those workloads.
          $vswhere = "${env:ProgramFiles(x86)}\Microsoft Visual Studio\Installer\vswhere.exe"
          $vs2026 = & $vswhere -version "[18.0,19.0)" -prerelease -products "*" -property installationPath 2>$null | Select-Object -First 1
          if ($vs2026) {
            Write-Host "VS 2026 already present at $vs2026"
          } else {
            Write-Host "Installing visualstudio2026buildtools-preview via chocolatey..."
            $params = "--add Microsoft.VisualStudio.Workload.VCTools --add Microsoft.VisualStudio.Component.VC.Tools.x86.x64 --add Microsoft.VisualStudio.Component.Windows11SDK.22621 --quiet --norestart --wait --nocache"
            # Retry on transient community.chocolatey.org outages (503/504
            # on V2 feed — chocolatey silently exits 0 on fetch failure,
            # so we retry based on observed install result, not exit code).
            # Each attempt: choco install → poll vswhere 60s → check present.
            $maxTries = 3
            $attempt = 0
            do {
              $attempt++
              Write-Host "choco install attempt $attempt/$maxTries..."
              choco install visualstudio2026buildtools-preview -y --pre --no-progress --package-parameters="$params"
              $rc = $LASTEXITCODE
              # Poll vswhere — Installer registration can lag a few seconds
              # after choco reports "installed" (exit 3010 = reboot-pending).
              for ($i = 0; $i -lt 12; $i++) {
                Start-Sleep -Seconds 5
                $vs2026 = & $vswhere -version "[18.0,19.0)" -prerelease -products "*" -property installationPath 2>$null | Select-Object -First 1
                if ($vs2026) { break }
              }
              if ($vs2026) { break }
              if ($attempt -lt $maxTries) {
                $backoff = 30 * $attempt
                Write-Host "Attempt $attempt did not produce VS 2026 (choco exit $rc). Retrying in ${backoff}s..."
                Start-Sleep -Seconds $backoff
              }
            } while ($attempt -lt $maxTries)
            if (-not $vs2026) {
              throw "VS 2026 not found after $maxTries chocolatey attempts (community.chocolatey.org may be degraded — re-run workflow when repo recovers)"
            }
          }
          Write-Host "VS 2026 installationPath: '$vs2026'"
          $mscvDir = Join-Path $vs2026 "VC\Tools\MSVC"
          Write-Host "Listing MSVC toolchains at: $mscvDir"
          if (-not (Test-Path $mscvDir)) { throw "VC\Tools\MSVC not found — VCTools workload missing" }
          $tools = Get-ChildItem $mscvDir -Directory -ErrorAction Stop | Sort-Object Name -Descending
          Write-Host "Found $($tools.Count) MSVC toolchain(s): $($tools.Name -join ', ')"
          if (-not $tools -or $tools.Count -eq 0) { throw "VC\Tools\MSVC is empty" }
          $toolName = $tools[0].Name
          Write-Host "Picking newest: $toolName"
          $verMatch = [regex]::Match($toolName, '^(\d+)\.(\d+)')
          if (-not $verMatch.Success) { throw "Cannot parse major.minor from toolchain name '$toolName'" }
          $major = [int]$verMatch.Groups[1].Value
          $minor = [int]$verMatch.Groups[2].Value
          Write-Host "Parsed major=$major minor=$minor"
          if ($major -lt 14 -or ($major -eq 14 -and $minor -lt 50)) {
            throw "VS 2026 has MSVC $toolName (major=$major, minor=$minor), < 14.50 required"
          }
          Write-Host "Toolchain OK, exporting VS2026_PATH"
          Add-Content -Path $env:GITHUB_ENV -Value "VS2026_PATH=$vs2026" -Encoding utf8
          Write-Host "DONE"
          # choco's exit 3010 (reboot-required) sets $LASTEXITCODE=3010 and
          # in pwsh 7.3+ with $PSNativeCommandUseErrorActionPreference=$true
          # the GitHub Actions wrapper returns non-zero at step end regardless
          # of subsequent logic. Explicit exit 0 overrides.
          exit 0

      - name: Install Vulkan SDK
        shell: pwsh
        run: |
          # Retry based on observed install result — chocolatey silently
          # exits 0 on V2 feed fetch failures (503/504), so we verify
          # C:\VulkanSDK exists rather than trusting the exit code.
          $maxTries = 3
          $attempt = 0
          $sdkDir = $null
          do {
            $attempt++
            Write-Host "choco install vulkan-sdk attempt $attempt/$maxTries..."
            choco install vulkan-sdk -y --no-progress
            $sdkDir = Get-ChildItem "C:\VulkanSDK" -Directory -ErrorAction SilentlyContinue |
                      Sort-Object Name -Descending | Select-Object -First 1
            if ($sdkDir) { break }
            if ($attempt -lt $maxTries) {
              $backoff = 30 * $attempt
              Write-Host "Attempt $attempt did not produce VulkanSDK. Retrying in ${backoff}s..."
              Start-Sleep -Seconds $backoff
            }
          } while ($attempt -lt $maxTries)
          if (-not $sdkDir) {
            throw "VulkanSDK not found after $maxTries chocolatey attempts (community.chocolatey.org may be degraded — re-run workflow when repo recovers)"
          }
          echo "VULKAN_SDK=$($sdkDir.FullName)" >> $env:GITHUB_ENV
          echo "$($sdkDir.FullName)\Bin" >> $env:GITHUB_PATH
          Write-Host "Vulkan SDK: $($sdkDir.FullName)"

      - name: Rust cache
        uses: Swatinem/rust-cache@v2
        with:
          workspaces: core
          shared-key: windows-staging

      - name: Fetch onnxruntime win-x64 DLL (Parakeet ort path)
        # ort 2.0.0-rc.10 binds to onnxruntime 1.22.0; the .csproj
        # CopyOrtDll target reads this exact path so it can co-locate
        # onnxruntime.dll next to dimmy_lib.dll in the publish bundle.
        # Same script the local dev flow uses (idempotent / cached).
        shell: pwsh
        run: scripts/download-onnxruntime.ps1

      - name: Build Rust DLL (VS 2026 MSVC env)
        working-directory: core
        shell: cmd
        env:
          LIBCLANG_PATH: "C:\\Program Files\\LLVM\\bin"
          CMAKE_GENERATOR: Ninja
          CARGO_TARGET_DIR: "D:\\t"
          # Diagnostic build (see macOS step for the rationale).
          POSTHOG_API_KEY: ${{ secrets.POSTHOG_API_KEY }}
          SENTRY_DSN: ${{ secrets.SENTRY_DSN }}
          # staging credentials — pubkey + URL are public by design (see
          # .env.staging committed to the repo). Hardcoding inline avoids
          # depending on a secrets.DIMMY_LICENSE_PUBKEY_STAGING entry
          # that may not be set on this repo's GitHub Secrets — the
          # private signing key lives only in Cloudflare's staging Worker
          # secret store, never in the public binary's verify pubkey.
          # build.rs invariant (commit 8a71f30) requires pubkey + URL
          # to move together; flavor=staging triggers ~/.config/dimmy-
          # staging isolation + UI watermark.
          DIMMY_LICENSE_PUBKEY: avlM65dRGDHomOzqGICiuIXf6_dY21w653AOnza2fzU
          DIMMY_LICENSE_SERVER_URL: https://license-staging.dimmy.app
          DIMMY_BUILD_FLAVOR: staging
        run: |
          REM Activate VS 2026 MSVC for this step only — PATH, INCLUDE,
          REM LIB point at the 14.50+ toolchain. Subsequent steps inherit
          REM the original job env (VS 2022) because activation is
          REM scoped to this shell invocation. cmd is required because
          REM vcvars64.bat is a batch script.
          call "%VS2026_PATH%\VC\Auxiliary\Build\vcvars64.bat"
          if errorlevel 1 exit /b 1
          REM ── DO NOT add `cargo clean --release` back here ──
          REM     Dropped 2026-05-14 to reuse the Swatinem/rust-cache@v2
          REM     restore — saves 20-25 min per Win build (full rebuild
          REM     of ~500 crates + whisper.cpp + ggml-vulkan shaders +
          REM     llama-cpp-sys + ort native bindings).
          REM     Safety net: the "Gate — verify dimmy_lib.dll linker
          REM     version" step below runs dumpbin on the produced DLL
          REM     and aborts if the linker is < 14.50 (the historical
          REM     miscompile threshold). So even if the cache contains
          REM     vintage objects, the gate blocks the release.
          REM     Memory entry: feedback_cargo_clean_dropped_from_win_ci.md
          REM     If a Win build EVER produces a broken DLL (linker
          REM     gate fail, test-install red, user-reported Win crash),
          REM     FIRST diagnostic = re-add `cargo clean --release` and
          REM     re-run. Then investigate why the cache went stale.
          cargo build --lib --release --features local-stt-vulkan,local-llm-vulkan,local-stt-parakeet,license-client

      - name: Gate — verify dimmy_lib.dll linker version
        shell: pwsh
        run: |
          # Hard gate: the whisper Vulkan backend miscompile lives in MSVC
          # linker <= 14.44. Any DLL linked with an older toolchain must NOT
          # ship. Extracts linker version from PE headers via dumpbin.
          # dumpbin is not in PATH here (the previous step's vcvars
          # activation was scoped to that cmd subshell), so locate it under
          # VS2026_PATH.
          $dll = "D:\t\release\dimmy_lib.dll"
          if (-not (Test-Path $dll)) { throw "dimmy_lib.dll not found at $dll" }
          $mscvDir = Join-Path $env:VS2026_PATH "VC\Tools\MSVC"
          $tool = Get-ChildItem $mscvDir -Directory | Sort-Object Name -Descending | Select-Object -First 1
          $dumpbin = Join-Path $tool.FullName "bin\Hostx64\x64\dumpbin.exe"
          if (-not (Test-Path $dumpbin)) { throw "dumpbin.exe not found at $dumpbin" }
          Write-Host "Using dumpbin at: $dumpbin"
          $out = & $dumpbin /headers $dll
          $line = $out | Select-String "linker version" | Select-Object -First 1
          if (-not $line) { throw "Could not find linker version in dumpbin output" }
          if ($line -notmatch '(\d+)\.(\d+)') { throw "Could not parse linker version from '$line'" }
          $major = [int]$Matches[1]
          $minor = [int]$Matches[2]
          Write-Host "dimmy_lib.dll linker version: $major.$minor"
          if ($major -lt 14 -or ($major -eq 14 -and $minor -lt 50)) {
            throw "dimmy_lib.dll linked with MSVC $major.$minor. Known whisper.cpp Vulkan codegen bug in <14.50. Aborting release."
          }
          exit 0

      - name: Setup .NET
        uses: actions/setup-dotnet@v5
        with:
          dotnet-version: '8.0.x'

      # Restore-only + explicit best-effort save (see release.yml for the
      # rationale): the combined actions/cache post-save tars
      # ~/.nuget/packages at teardown while build-server processes still
      # hold Windows file locks → tar fails → job red after the binary is
      # already built. Save right after restore instead, continue-on-error.
      - name: Restore NuGet cache
        id: nuget-cache
        uses: actions/cache/restore@v5
        with:
          path: ~/.nuget/packages
          key: ${{ runner.os }}-nuget-${{ hashFiles('**/*.csproj') }}
          restore-keys: ${{ runner.os }}-nuget-

      - name: Restore NuGet packages
        working-directory: platforms/windows/Dimmy.Windows
        run: dotnet restore

      - name: Save NuGet cache
        if: steps.nuget-cache.outputs.cache-hit != 'true'
        continue-on-error: true
        uses: actions/cache/save@v5
        with:
          path: ~/.nuget/packages
          key: ${{ runner.os }}-nuget-${{ hashFiles('**/*.csproj') }}

      - name: Locate VS AppxPackage tools (for MrtCore PRI generation)
        shell: pwsh
        run: |
          # MrtCore.PriGen.targets references Microsoft.Build.Packaging.Pri.Tasks.dll
          # which only ships with the VS AppxPackage workload, not the .NET SDK.
          # Without it, resources.pri is not generated and the app runs headless at
          # runtime (every ms-appx:///Views/*.xaml URI fails to resolve).
          # Pin to VS 2022 (17.x) — the side-by-side VS 2026 BuildTools used
          # for the Rust build lacks UWP/AppxPackage workloads entirely, so
          # a naive `-latest` would pick VS 2026 and this step would fail.
          # Accept either VS 2022 (17.x) or VS 2026 Enterprise (18.x).
          # The candidates array below probes both v17.0/AppxPackage and
          # v18.0/AppxPackage subpaths, so vswhere just needs to surface
          # ANY VS install with the UWP workload. On the new
          # windows-2025-vs2026 runner image VS 2022 is gone and VS 2026
          # Enterprise ships in its place — restricting to [17.0,18.0)
          # would fail there.
          $vs = & "${env:ProgramFiles(x86)}\Microsoft Visual Studio\Installer\vswhere.exe" -version "[17.0,19.0)" -prerelease -products "*" -property installationPath | Select-Object -First 1
          if (-not $vs) { throw "vswhere did not find a VS 2022 or VS 2026 install" }
          $candidates = @(
            "$vs\MSBuild\Microsoft\VisualStudio\v17.0\AppxPackage\",
            "$vs\MSBuild\Microsoft\VisualStudio\v18.0\AppxPackage\"
          )
          $found = $candidates | Where-Object { Test-Path (Join-Path $_ "Microsoft.Build.Packaging.Pri.Tasks.dll") } | Select-Object -First 1
          if (-not $found) {
            Write-Host "VS install at: $vs"
            Write-Host "Probed paths:"
            $candidates | ForEach-Object { Write-Host "  - $_" }
            throw "Microsoft.Build.Packaging.Pri.Tasks.dll not found in VS install. UWP workload missing?"
          }
          Write-Host "AppxMSBuildToolsPath = $found"
          echo "AppxMSBuildToolsPath=$found" >> $env:GITHUB_ENV

      - name: Publish Windows app (self-contained)
        working-directory: platforms/windows/Dimmy.Windows
        run: dotnet publish Dimmy.Windows.csproj -c Release -r win-x64 --self-contained true -p:WindowsAppSDKSelfContained=true -p:SelfContained=true "-p:AppxMSBuildToolsPath=$env:AppxMSBuildToolsPath" -o publish

      - name: Prepare distribution
        shell: pwsh
        run: |
          # No co-located VC++ runtime: Velopack `--framework vcredist143-x64`
          # already installs the official Microsoft VC Redist at setup time,
          # which lands in System32. Shipping a second copy next to
          # dimmy_lib.dll is at best redundant and at worst overrides System32
          # with an older ABI (v0.6.10 crash cause). System32 wins.
          $pubDir = "platforms/windows/Dimmy.Windows/publish"
          Copy-Item "D:\t\release\dimmy_lib.dll" "$pubDir/"
          # onnxruntime.dll + onnxruntime_providers_shared.dll are the
          # Parakeet ort path's runtime dependency. The .csproj target
          # copies them to bin/ during dotnet build but `dotnet publish`
          # output landed in `publish/` — copy them across explicitly so
          # the self-contained bundle has everything Parakeet needs.
          $ortLib = "core/target/onnxruntime-win-x64-1.22.0/lib"
          Copy-Item "$ortLib/onnxruntime.dll" "$pubDir/"
          Copy-Item "$ortLib/onnxruntime_providers_shared.dll" "$pubDir/"
          Get-ChildItem "$pubDir" -Filter "*.pdb" -Recurse | Remove-Item -ErrorAction SilentlyContinue

      - name: Verify self-contained bundle
        shell: pwsh
        run: platforms/windows/verify-self-contained.ps1 -Path platforms/windows/Dimmy.Windows/publish

      - name: Run Windows tests
        working-directory: platforms/windows/Dimmy.Windows.Tests
        run: dotnet test -c Release --logger "console;verbosity=normal"

      - name: Install Velopack CLI
        run: dotnet tool install -g vpk

      - name: Package with Velopack (self-contained + VC++ redist)
        shell: pwsh
        run: |
          $version = "${{ needs.create-release.outputs.version }}"
          # Velopack needs semver (X.Y.Z) — extract base version before any suffix
          if ($version -match '^(\d+\.\d+\.\d+)') { $semver = $Matches[1] } else { $semver = "0.0.1" }
          $pubDir = (Resolve-Path "platforms/windows/Dimmy.Windows/publish").Path
          Write-Host "Packing Velopack v$semver from $pubDir"
          vpk pack --packId Dimmy --packVersion $semver --packDir "$pubDir" --mainExe Dimmy.Windows.exe --icon "platforms/windows/Dimmy.Windows/Assets/dimmy.ico" --framework "vcredist143-x64" --outputDir platforms/windows/velopack-output
          Get-ChildItem platforms/windows/velopack-output | ForEach-Object { Write-Host "$($_.Name) ($([math]::Round($_.Length/1MB, 1)) MB)" }

      - name: Upload to release
        uses: softprops/action-gh-release@v3
        with:
          tag_name: staging-latest
          # Upload the full Velopack output set, not just the
          # human-friendly installer / portable. UpdateService on the
          # client side calls `GithubSource(prerelease: true)` which
          # parses `releases.win.json` to find the latest delta/full
          # .nupkg — without these files we get "Setup.exe in the
          # release but no manifest" and auto-update silently no-ops.
          # Pattern matches what `vpk pack` writes into velopack-output:
          #   - Setup.exe / Portable.zip — user-facing
          #   - releases.win.json — manifest Velopack polls
          #   - Dimmy-{ver}-full.nupkg — fallback full package
          #   - Dimmy-{ver}-delta.nupkg — delta from previous release
          # Burned 2026-05-11: staging had Setup but no manifest, so
          # the "Pre-release" channel found nothing to download.
          files: |
            platforms/windows/velopack-output/Dimmy-win-Setup.exe
            platforms/windows/velopack-output/Dimmy-win-Portable.zip
            platforms/windows/velopack-output/releases.win.json
            platforms/windows/velopack-output/Dimmy-*-full.nupkg
            platforms/windows/velopack-output/Dimmy-*-delta.nupkg

  # ── Linux Native Build (parallel with macOS + Windows + rust-check) ──
  build-linux:
    name: Build Linux (GTK4)
    needs: [create-release]
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v5

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          components: clippy

      - name: Cache cargo
        uses: actions/cache@v5
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            platforms/linux/target
          key: linux-cargo-${{ hashFiles('platforms/linux/Cargo.lock') }}
          restore-keys: linux-cargo-

      - name: Install GTK4 + libadwaita dev packages
        run: |
          sudo apt-get update
          sudo apt-get install -y libgtk-4-dev libadwaita-1-dev libdbus-1-dev pkg-config \
            libasound2-dev libxdo-dev libfuse2 cmake

      - name: Clippy
        working-directory: platforms/linux
        run: cargo clippy -- -D warnings

      - name: Test
        working-directory: platforms/linux
        run: cargo test

      - name: Build release
        working-directory: platforms/linux
        env:
          # Diagnostic build (see macOS step for the rationale).
          POSTHOG_API_KEY: ${{ secrets.POSTHOG_API_KEY }}
          SENTRY_DSN: ${{ secrets.SENTRY_DSN }}
          # staging credentials — pubkey + URL are public by design (see
          # .env.staging committed to the repo). Hardcoding inline avoids
          # depending on a secrets.DIMMY_LICENSE_PUBKEY_STAGING entry
          # that may not be set on this repo's GitHub Secrets — the
          # private signing key lives only in Cloudflare's staging Worker
          # secret store, never in the public binary's verify pubkey.
          # build.rs invariant (commit 8a71f30) requires pubkey + URL
          # to move together; flavor=staging triggers ~/.config/dimmy-
          # staging isolation + UI watermark.
          DIMMY_LICENSE_PUBKEY: avlM65dRGDHomOzqGICiuIXf6_dY21w653AOnza2fzU
          DIMMY_LICENSE_SERVER_URL: https://license-staging.dimmy.app
          DIMMY_BUILD_FLAVOR: staging
        run: cargo build --release --features license-client

      - name: Package AppImage
        run: |
          # Download linuxdeploy + GTK plugin
          wget -q https://github.com/linuxdeploy/linuxdeploy/releases/download/continuous/linuxdeploy-x86_64.AppImage
          wget -q https://raw.githubusercontent.com/linuxdeploy/linuxdeploy-plugin-gtk/master/linuxdeploy-plugin-gtk.sh
          chmod +x linuxdeploy-x86_64.AppImage linuxdeploy-plugin-gtk.sh

          # Build AppDir structure
          mkdir -p AppDir/usr/bin AppDir/usr/share/applications AppDir/usr/share/icons/hicolor/scalable/apps
          cp platforms/linux/target/release/dimmy-linux AppDir/usr/bin/
          cp platforms/linux/assets/com.dimmy.app.desktop AppDir/usr/share/applications/
          cp platforms/linux/assets/com.dimmy.app.svg AppDir/usr/share/icons/hicolor/scalable/apps/

          # Create AppImage with GTK plugin (bundles libgtk-4, libadwaita, etc.)
          export DEPLOY_GTK_VERSION=4
          ./linuxdeploy-x86_64.AppImage \
            --appdir AppDir \
            --plugin gtk \
            --desktop-file platforms/linux/assets/com.dimmy.app.desktop \
            --icon-file platforms/linux/assets/com.dimmy.app.svg \
            --output appimage

          # Rename to consistent name
          mv Dimmy*.AppImage Dimmy-native-linux-x86_64.AppImage

      - name: Upload AppImage to release
        uses: softprops/action-gh-release@v3
        with:
          tag_name: staging-latest
          files: Dimmy-native-linux-x86_64.AppImage

      - name: Upload raw binary (artifact)
        uses: actions/upload-artifact@v5
        with:
          name: dimmy-linux
          path: platforms/linux/target/release/dimmy-linux

  # ── Clean-machine install test ──────────────────────────────────────
  # Gates publish: if the installer is broken on a fresh Windows runner,
  # the staging release stays in draft and never reaches testers.
  test-install:
    name: Test Install (Clean Windows)
    needs: build-windows
    uses: ./.github/workflows/test-install.yml
    permissions:
      contents: write
    with:
      tag: staging-latest

  # ── Publish (waits for all jobs including rust-check + test-install) ─
  publish:
    name: Publish Staging Release
    needs: [rust-check, create-release, build-macos, build-windows, build-linux, test-install]
    runs-on: ubuntu-latest
    steps:
      - name: Publish release
        uses: actions/github-script@v8
        with:
          script: |
            await github.rest.repos.updateRelease({
              owner: context.repo.owner,
              repo: context.repo.repo,
              release_id: ${{ needs.create-release.outputs.release_id }},
              draft: false,
              prerelease: true
            });
